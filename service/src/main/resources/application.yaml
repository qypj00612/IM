server:
  port: 8000

spring:
  application:
    name: business
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://192.168.181.128:3306/im-core?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Ypj.,?/0102
  data:
    redis:
      host: 192.168.181.128
      port: 6379
      password: 123

mybatis-plus:
  mapper-locations: classpath:**/mapper/*.xml
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
  global-config:
    db-config:
      update-strategy: not_null
      id-type: NONE
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.lld.im.service.user.dao: debug
    com.baomidou.mybatisplus: debug

rocketmq:
  name-server: 192.168.181.128:9876
  producer:
    group: default
    send-message-timeout: 15000  # 延长到15秒，适配容器
    retry-times-when-send-failed: 5  # 增加重试
    retry-times-when-send-async-failed: 5
    max-message-size: 4194304  # 增大消息体限制（避免大消息被拒绝）
  consumer:
    pull-batch-size: 10000

config:
  privateKey: 123456
  server-addr: 192.168.181.128:8848
  namespace: c65a45b8-9b55-4b53-9e10-fbab776f8db6
  username: nacos
  password: nacos
  im-route-way: 3 # 1是随机 2是轮询 3是hash
  consistent-hash-way: 1 # 具体的hash算法 1是TreeMap 2是其他
  call-back-url: http://127.0.0.1:8000/callback
  sendMessageCheckFriend: true # 发送消息是否校验关系链
  sendMessageCheckBlack: true # 发送消息是否校验黑名单
  modifyUserAfterCallback: false # 用户资料变更之后回调开关
  addFriendAfterCallback: false # 添加好友之后回调开关
  addFriendBeforeCallback: false # 添加好友之前回调开关
  modifyFriendAfterCallback: false # 修改好友之后回调开关
  deleteFriendAfterCallback: false # 删除好友之后回调开关
  addFriendShipBlackAfterCallback: false #添加黑名单之后回调开关
  deleteFriendShipBlackAfterCallback: false #删除黑名单之后回调开关
  createGroupAfterCallback: false # 创建群聊之后回调开关
  modifyGroupAfterCallback: false # 修改群聊之后回调开关
  destroyGroupAfterCallback: false # 解散群聊之后回调开关
  deleteGroupMemberAfterCallback: false # 删除群成员之后回调
  addGroupMemberAfterCallback: false # 拉人入群之后回调
  addGroupMemberBeforeCallback: false # 拉人入群之前回调
  sendMessageAfterCallback: false # 发送单聊消息之后
  sendMessageBeforeCallback: false # 发送单聊消息之前
  sendGroupMessageAfterCallback: false # 发送群聊消息之后
  sendGroupMessageBeforeCallback: false # 发送群聊消息之前
  offlineMessageCount: 1000 #离线消息存储条数
  deleteConversationSyncMode: 1 #1多段同步

httpclient:
  maxTotal: 100
  defaultMaxPerRoute: 50
  connectTimeout: 2000
  connectionRequestTimeout: 2000
  socketTimeout: 5000
  staleConnectionCheckEnabled: true